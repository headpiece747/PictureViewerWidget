<UserControl x:Class="PictureViewerWidget.PictureViewerWidgetSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:Background="White"
             d:DesignHeight="80" d:DesignWidth="225" Margin="10, 25">
    <StackPanel>

        <!-- Folder path row -->
        <Grid Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Content="Picture Folder" FontSize="10" HorizontalAlignment="Stretch"/>
            <Button x:Name="BtnBrowse" Grid.Column="1" FontSize="10"
                    HorizontalAlignment="Stretch" Click="BtnBrowse_Click"/>
        </Grid>

    </StackPanel>
</UserControl>
